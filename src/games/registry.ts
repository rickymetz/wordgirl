import type { GameDefinition } from "./types";
import { polygram } from "./polygram";

/** Adding a game = new folder under games/ + one line here. */
export const games: GameDefinition[] = [polygram];
